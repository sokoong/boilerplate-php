app.controller("AppController", function ($scope, $http, $timeout) {
    $scope.hello = "Hello";
    // var req = $.param({ id: 1 });
    // $http.post("", req, httpconfig).then(function(res) {
    //     console.log("res", res);
    // });
});

